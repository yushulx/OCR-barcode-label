language: cpp

jobs:
  include:
    - os: linux
      arch: amd64
      before_install:
        - sudo apt install libopencv-dev

branches:
  only:
    - main

script:
    - mkdir build
    - cd build
    - cmake ..
    - cmake --build . --config release